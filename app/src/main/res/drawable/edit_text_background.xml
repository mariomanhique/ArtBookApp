<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Set your desired background color -->
    <corners android:radius="10dp" /> <!-- Adjust the radius as needed -->
    <stroke android:width="1dp" android:color="@android:color/darker_gray" /> <!-- Optional: add a border stroke -->
</shape>